# ── PyQt device app (household user) ──────────────────────────────────
pyaudio
numpy
SpeechRecognition
pyttsx3
PyGetWindow
PyQt6
requests           # PyQt → backend HTTP dispatch

# ── FastAPI backend (responder + admin interfaces) ─────────────────────
fastapi
uvicorn[standard]
sqlalchemy[asyncio]
asyncpg
python-dotenv
python-jose[cryptography]
python-multipart
